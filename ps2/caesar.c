#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    if (argc != 2)
    {
        printf("Invalid number of command line arguments entered.\n\n");
        printf("Usage: caesar <key>\n");
        printf("where <key> is a numeric key between 1 and 26.\n\n"); 
    }
}
